#include <iostream>
#include <string>
#include <algorithm>
#include <queue>
#include <cmath>
#include <vector>

using namespace std;

int main() {
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	int t;
	priority_queue<pair<int,int>,vector<pair<int,int> >,greater<int>> pq;
	while(t--) {
		
	}	
}
